const milliseconds = 1000;
const seconds = 5;
const config = {
	countStart: 0,
	increment: 1,
	refreshIDLength: 8,
	idLength: 16,
	minimumTaskCount: 3,
	tickerDelay: milliseconds * seconds,
};

export default config;
